# Loan Types Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a mobile-friendly Loan Types management screen matching the supplied reference.

**Architecture:** Keep the screen self-contained in `loan_type.dart` with one local sample loan type matching the image. Render it as a mobile card with the same fields and local add/edit/delete actions.

**Tech Stack:** Flutter/Dart, Material 3, existing `AppConstant.primarycolor`.

## Global Constraints

- No API, database, or persistence.
- Initialize with the sample row shown in the reference.
- Use a vertically scrollable mobile layout instead of the desktop table.

---

### Task 1: Implement Loan Types screen

**Files:**
- Modify: `lib/Screens/Loan_management/loan_type.dart`

**Interfaces:**
- Produces `LoanTypeScreen`, usable from Loan Management navigation.

- [ ] Add header and Add Loan Type action.
- [ ] Add local sample loan type card with amount, tenure, allowance, status, and actions.
- [ ] Add local add/edit form and delete confirmation.
- [ ] Format and inspect the file.

### Task 2: Verify

**Files:**
- Test: Flutter analyzer and existing test suite

- [ ] Run `flutter analyze`.
- [ ] Run `flutter test`.
- [ ] Run `git diff --check`.
