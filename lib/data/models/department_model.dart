class Department {
  int id;
  String name;
  String description;
  bool isActive;

  Department({
    required this.id,
    required this.name,
    required this.description,
    required this.isActive,
  });
}
